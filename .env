# Domain
# This would be set to the production domain with an env var on deployment
# used by Traefik to transmit traffic and aqcuire TLS certificates
DOMAIN=localhost
# To test the local Traefik config
# DOMAIN=localhost.tiangolo.com

# Used by the backend to generate links in emails to the frontend
FRONTEND_HOST=http://localhost:5173
# In staging and production, set this env var to the frontend host, e.g.
# FRONTEND_HOST=https://dashboard.example.com

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="Oh My Coins (OMC!)"
STACK_NAME=ohmycoins

# Backend
BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173,http://localhost.tiangolo.com"
SECRET_KEY=yHimxsRpn9K8GGPQ
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=yJp0m7zdwWbVNVUD

# Emails
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=yCF1VKX3h6mQxrNQ

# Automatic Database Seeding (Dev Environment)
AUTO_SEED_DB=true  # Set to false to disable automatic seeding
SEED_USER_COUNT=10  # Number of test users to generate
SEED_ALGORITHM_COUNT=15  # Number of test algorithms to generate
SEED_AGENT_SESSION_COUNT=20  # Number of agent sessions to generate
SEED_COLLECT_REAL_DATA=true  # Set to false to skip API calls (faster seeding)

# Redis (Phase 3 - Agent State Management)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=  # Optional, uncomment if you set a password

# LLM Provider Configuration (Phase 3 - Agentic AI)
LLM_PROVIDER=openai  # Options: openai, anthropic, azure, local
OPENAI_API_KEY=  # Set your OpenAI API key here
OPENAI_MODEL=gpt-4-turbo-preview
# ANTHROPIC_API_KEY=  # Uncomment if using Anthropic
# ANTHROPIC_MODEL=claude-3-sonnet-20240229
MAX_TOKENS_PER_REQUEST=4000
ENABLE_STREAMING=True

# Agent System Configuration
AGENT_MAX_ITERATIONS=10
AGENT_TIMEOUT_SECONDS=300
AGENT_CODE_EXECUTION_TIMEOUT=60

# Coinspot Configuration
# When true: Uses web scraping for 538+ coins
# When false: Uses public API for 17 major coins only
COINSPOT_USE_WEB_SCRAPING=true

SENTRY_DSN=

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend

DOCKER_IMAGE_BACKEND=ghcr.io/marklimmage/ohmycoins-backend
DOCKER_IMAGE_FRONTEND=ghcr.io/marklimmage/ohmycoins-frontend