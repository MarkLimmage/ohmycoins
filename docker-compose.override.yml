services:
  backend:
    environment:
      - POSTGRES_PORT=5432
  orchestrator:
    environment:
      - POSTGRES_PORT=5432
  prestart:
    environment:
      - POSTGRES_PORT=5432
  celery_worker:
    environment:
      - POSTGRES_PORT=5432
